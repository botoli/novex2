# 🚀 Startup Project – Full‑Stack Management Platform

![React](https://img.shields.io/badge/React-19.2.0-61DAFB?logo=react&logoColor=white)
![TypeScript](https://img.shields.io/badge/TypeScript-5.9.3-3178C6?logo=typescript&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-7.2.4-646CFF?logo=vite&logoColor=white)
![Laravel](https://img.shields.io/badge/Laravel-12.40-FF2D20?logo=laravel&logoColor=white)
![Redux Toolkit](https://img.shields.io/badge/Redux_Toolkit-2.11.0-764ABC?logo=redux&logoColor=white)
![Sass](https://img.shields.io/badge/Sass-1.94.2-CC6699?logo=sass&logoColor=white)

**Современная full‑stack платформа для управления проектами, задачами и командной работой.**  
Фронтенд на React + TypeScript + Vite, бэкенд на Laravel (PHP), с поддержкой AI‑ассистента, Redux‑состояния, модульной архитектуры и красивого UI.

---

## 📋 Содержание

- [Особенности](#особенности)
- [Структура проекта](#структура-проекта)
- [Технологический стек](#технологический-стек)
- [Быстрый старт](#быстрый-старт)
  - [Предварительные требования](#предварительные-требования)
  - [Установка и запуск фронтенда](#установка-и-запуск-фронтенда)
  - [Установка и запуск бэкенда](#установка-и-запуск-бэкенда)
- [Скрипты](#скрипты)
- [Архитектура фронтенда](#архитектура-фронтенда)
- [Архитектура бэкенда](#архитектура-бэкенда)
- [Конфигурация ESLint](#конфигурация-eslint)
- [Деплой](#деплой)
- [Лицензия](#лицензия)

---

## ✨ Особенности

- **Полнофункциональный дашборд** с графиками, статистикой и быстрым доступом
- **Управление проектами** – создание, редактирование, просмотр деталей, назначение участников
- **Система задач** с прикреплением файлов, статусами, сроками
- **AI‑ассистент** (интеграция через сервис `ai.ts`) для помощи в планировании
- **Командный чат** в реальном времени (заготовка)
- **Календарь и расписание** (компонент Schedule)
- **Быстрые заметки** (QuickNote) с сохранением в локальное хранилище
- **Панель навигации (LeftPanel)** с темами и иконками
- **Адаптивный дизайн** на SCSS‑модулях с кастомными переменными
- **State‑менеджмент** через Redux Toolkit + Redux Persist
- **Авторизация/регистрация** (формы готовы, бэкенд на Laravel Sanctum)
- **REST API** с CORS‑поддержкой и миграциями базы данных

---

## 📁 Структура проекта

```
startup/
├── frontend/ (корень проекта)
│   ├── public/              # Статические ресурсы
│   ├── src/
│   │   ├── assets/          # Иконки, шрифты, изображения
│   │   ├── Components/      # React‑компоненты
│   │   │   ├── Errors/      # Страницы ошибок (404)
│   │   │   ├── Form/        # Формы авторизации, регистрации
│   │   │   └── Main/        # Основные страницы (Dashboard, Projects, AI и т.д.)
│   │   ├── constants/       # Константы (quickAccess)
│   │   ├── services/        # API‑клиент, AI‑сервис
│   │   ├── store/           # Redux‑сторе (user, index)
│   │   ├── style/           # SCSS‑модули
│   │   └── App.tsx, main.tsx
│   ├── index.html
│   ├── vite.config.ts
│   ├── tsconfig.*.json
│   ├── package.json
│   └── README.md
│
└── backend/                 # Laravel‑приложение
    ├── app/
    │   ├── Http/Controllers/ # Контроллеры (Auth, Project, Task…)
    │   ├── Models/           # Eloquent‑модели
    │   └── Providers/
    ├── config/               # Конфигурации (cors, database, auth…)
    ├── database/migrations/  # Миграции БД
    ├── routes/api.php        # API‑маршруты
    ├── public/               # Точка входа Laravel
    ├── composer.json
    └── README.md
```

---

## 🛠 Технологический стек

### Фронтенд
- **React 19** – библиотека компонентов
- **TypeScript** – типизация
- **Vite** – сборка и dev‑сервер
- **Redux Toolkit + Redux Persist** – управление состоянием
- **React Router DOM v7** – навигация
- **Sass/SCSS** – стили с модулями
- **Bootstrap 5** – утилиты сетки
- **EmailJS** – отправка email
- **SVGR** – импорт SVG как React‑компоненты

### Бэкенд
- **Laravel 12** – PHP‑фреймворк
- **Laravel Sanctum** – аутентификация API
- **MySQL/PostgreSQL/SQLite** – база данных (через миграции)
- **CORS** – кросс‑доменные запросы
- **Guzzle** – HTTP‑клиент

### Инструменты
- **ESLint** – линтинг
- **Prettier** – форматирование (опционально)
- **Git** – контроль версий

---

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** 18+ и **npm**/**yarn**/**pnpm**
- **PHP** 8.2+ и **Composer**
- **База данных** (MySQL, PostgreSQL или SQLite)

### Установка и запуск фронтенда

1. **Клонируйте репозиторий** (если ещё не склонирован)
   ```bash
   git clone <repository-url>
   cd startup
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Запустите dev‑сервер**
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу [http://localhost:5173](http://localhost:5173).

4. **Сборка для production**
   ```bash
   npm run build
   npm run preview
   ```

### Установка и запуск бэкенда

1. **Перейдите в папку бэкенда**
   ```bash
   cd backend
   ```

2. **Установите PHP‑зависимости**
   ```bash
   composer install
   ```

3. **Настройте окружение**
   - Скопируйте `.env.example` в `.env`
   - Сгенерируйте ключ приложения:
     ```bash
     php artisan key:generate
     ```
   - Настройте подключение к БД в `.env` (переменные `DB_*`)

4. **Запустите миграции**
   ```bash
   php artisan migrate
   ```

5. **Запустите сервер разработки**
   ```bash
   php artisan serve
   ```
   API будет доступно по адресу [http://localhost:8000](http://localhost:8000).

6. **Опционально: сгенерируйте токены Sanctum**
   ```bash
   php artisan sanctum:install
   ```

---

## 📜 Скрипты

В корне фронтенда (`package.json`):

| Скрипт       | Действие                              |
|--------------|---------------------------------------|
| `npm run dev`| Запуск dev‑сервера Vite               |
| `npm run build`| Сборка проекта в `dist/`             |
| `npm run preview`| Предпросмотр собранного проекта    |
| `npm run lint`| Запуск ESLint                         |

В папке бэкенда (`composer.json`):

| Скрипт                | Действие                              |
|-----------------------|---------------------------------------|
| `php artisan serve`   | Запуск встроенного PHP‑сервера        |
| `php artisan migrate` | Выполнение миграций БД                |
| `composer test`       | Запуск тестов (если настроены)        |

---

## 🏗 Архитектура фронтенда

### Компоненты
- **Dashboard** – главная страница с виджетами
- **ProjectsPage** – список проектов
- **ProjectDetailPage** – детали проекта
- **TasksPage** – управление задачами
- **AIAssistantPage** – интерфейс AI‑помощника
- **TeamChat** – чат (заготовка)
- **Schedule** – календарь событий
- **QuickNote** – заметки
- **LeftPanel** – боковая панель навигации
- **Hero** – промо‑блок

### Стили
Используются **SCSS‑модули** (`.module.scss`), что обеспечивает изоляцию стилей.  
Общие переменные вынесены в `src/style/Main/_variables.module.scss`.

### State‑менеджмент
- **Redux Toolkit** – централизованный стор
- **Redux Persist** – сохранение состояния в `localStorage`
- **Слайсы**: `user` (данные пользователя), можно расширить под `projects`, `tasks`.

### Маршрутизация
React Router DOM настроен в `App.tsx` (или `main.tsx`).  
Поддерживаются страницы:
- `/` – Dashboard
- `/projects` – ProjectsPage
- `/projects/:id` – ProjectDetailPage
- `/tasks` – TasksPage
- `/ai` – AIAssistantPage
- `/schedule` – Schedule
- `/team` – TeamChat
- `/settings` – SettingsPage
- `/login`, `/register` – формы авторизации

---

## 🗄 Архитектура бэкенда

### Контроллеры
- `AuthController` – регистрация, вход, выход
- `ProjectController` – CRUD проектов
- `TaskController` – управление задачами
- `ProjectUserController` – участники проектов
- `TaskAttachment` – вложения к задачам

### Модели
- `User`, `Project`, `Task`, `ProjectUser`, `Task_attachment`

### Миграции
Созданы таблицы:
- `users`, `projects`, `project_user`, `tasks`, `task_attachments`, `personal_access_tokens`

### API‑маршруты
Определены в `routes/api.php`:
- `POST /api/register`, `/api/login`
- `GET /api/projects`, `POST /api/projects`, `PUT /api/projects/{id}`, `DELETE /api/projects/{id}`
- Аналогичные для задач, вложений и т.д.

### CORS
Настроен middleware `Cors.php` для разрешения запросов с фронтенда.

---

## 🔧 Конфигурация ESLint

Проект использует современную конфигурацию ESLint (файл `eslint.config.js`).  
Для расширения линтинга React‑компонентов можно добавить плагины:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // … другие конфиги
      reactX.configs['recommended-typescript'],
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
])
```

Подробности см. в разделе **Expanding the ESLint configuration** оригинального README.

---

## ☁ Деплой

### Фронтенд
- **Vercel**, **Netlify**, **Cloudflare Pages** – статический хостинг
- **Docker** – контейнеризация (можно создать `Dockerfile` на основе `node:18‑alpine`)

### Бэкенд
- **Laravel Forge**, **Ploi** – управление серверами
- **Heroku** (с buildpack `heroku/heroku‑buildpack‑php`)
- **Docker** – официальный образ `php:8.2‑apache` с настройкой Laravel

### База данных
- **MySQL** (например, через ClearDB на Heroku)
- **PostgreSQL** (например, через Supabase)
- **SQLite** – для лёгких развёртываний

---

## 📄 Лицензия

Этот проект лицензирован под [MIT License](LICENSE) (если файл лицензии отсутствует, можно добавить).

---

## 👥 Участие

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b feature/amazing‑feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing‑feature`)
5. Откройте Pull Request

---

## 📞 Контакты

По вопросам и предложениям – создавайте issue в репозитории или свяжитесь с разработчиком.

---

*Этот README обновлён под полный стек проекта. Актуально на декабрь 2025.*